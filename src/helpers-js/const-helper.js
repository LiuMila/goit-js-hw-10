const variable = {
  loaded: document.querySelector('.mask'),
  select: document.querySelector('#selectElement'),
  infoCat: document.querySelector('.cat-info'),
};

const { select, loaded, infoCat } = variable;

export { select, loaded, infoCat };